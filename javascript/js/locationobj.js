//location object

/*
1. window.location.Href = tells the url of the page
2. window.location.Hostname = tells the hostname of the page
3. window.location.pathname = tells the pathname of the current page
4. window.location.host = tells the host of the page
5. window.location.assign() = set the redirect location for the link
*/

//making a function for window objects
function myfunc() {
    // document.write(window.location.href); 
    // document.write(window.location.hostname);
    // document.write(window.location.pathname);
    // document.write(window.location.host);
    // document.write(window.location.assign())
    // window.location.assign('https://www.w3schools.com/tags/tag_p.asp')
}


